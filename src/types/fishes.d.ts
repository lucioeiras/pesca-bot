export type Fish = {
  name: string,
  genus: string,
  species: string,
  maxLength: number,
  commonLength: number,
  maxWeight: number,
  depthMax: number,
  vulnerability: number
}

export type Fishes = Fish[];
